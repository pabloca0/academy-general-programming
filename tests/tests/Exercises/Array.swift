//
//  Array.swift
//  tests
//
//  Created by Fernando Salom Carratala on 8/9/22.
//

import Foundation
import UIKit

class ArrayExercise {
    /*:
     ## Ejecicio 1
     Crea una función que dado un array de elementos inserte un número en una posición designada. Devuelve el nuevo array o nil en caso de que no sea posible
    */
    func test1(elements: [Int], number: Int, position: Int) -> [Int]?{
        return []
    }

    /*:
     ## Ejecicio 2
     Crea una función que dado un conjunto de enteros devuelva si existe un número dentro de él
     */
    func test2(elements: [Int], search: Int) -> Bool{
        return false
    }

    /*:
     ## Ejecicio 3
     Crea una función que dado un conjunto de enteros devuelva el número de veces que se repite un número dado
     */
    func test3(elements: [Int], search: Int) -> Int {
        return 0
    }

    /*:
     ## Ejecicio 4
     Crea una función que dado un conjunto de enteros los ordene de mayor a menor y devuelva el array resultante
     */
    func test4(elements: [Int]) -> [Int] {
        return []
    }

    /*:
     ## Ejecicio 5
     Crea una función que dado un conjunto de String añada un nuevo elemento que se le pase y devuelva el nuevo array
     */
    func test5(elements: [String], element: String) -> [String] {
        return []
    }
}
